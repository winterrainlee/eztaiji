# eztaiji 확장 계획

- 목적: eztaiji를 단순한 자세 도해가 아니라, **좌표 → 신체 물리 → 음양 → 무술적 의미**를 함께 읽는 개인 수련 도구로 확장한다.
- 진행 원칙: 한 번에 많은 기능을 넣지 않고, 아래 항목을 하나씩 구현·검증한 뒤 체크한다.
- 기준: 실제 수련과 사부님의 지도를 우선하고, 자료에서 확인되지 않은 내용은 추론이나 예시임을 명확히 표시한다.

## 0. 진행 방식

- [x] 확장 계획을 문서화한다.
- [ ] 각 단계는 구현 전 정의를 먼저 확정한다.
- [ ] 구현 후 실제 預備式 데이터로 동작을 검증한다.
- [ ] 다음 단계로 넘어가기 전에 문서와 코드가 서로 일치하는지 확인한다.

---

## 1. 시작 상태 개념 정의

목표: `0동작`이라는 표현 대신, 각 자세의 **시작 상태**를 명시한다.

### 원칙

- 시작 상태는 새로운 동작 번호가 아니다.
- 일반적으로 `이전 자세의 마지막 상태 = 다음 자세의 시작 상태`로 본다.
- 첫 자세인 預備式만 이전 자세를 참조하지 않고 `권가 시작 전 상태`를 별도로 가진다.
- UI에는 `0동작`이 아니라 `시작 상태`라고 표시한다.
- 필요하면 설명에 `이전 자세 ○○의 마지막 상태`라고 덧붙인다.

### 체크

- [x] `coordinate-system.md`에 시작 상태 정의를 추가한다.
- [x] `routine-navigation.md`에 탐색 규칙을 추가한다.
- [x] 첫 자세의 초기 상태를 어떻게 표현할지 확정한다. — `권가 시작 전 상태`로 표시하고 확인된 값만 기록한다.
- [x] 이후 자세가 이전 자세의 마지막 상태를 참조하는 규칙을 확정한다. — 같은 물리 상태를 새 자세의 출발점으로 참조한다.

---

## 2. 범용 동작·상태 데이터 구조 설계

목표: 預備式 전용 데이터 구조를 전체 64자세에 사용할 수 있는 구조로 바꾼다.

설계 기준은 [동작·상태 데이터 모델 0.1](data-model.md)이다. 기존의 한 프레임에 모든 정보를 넣는 개념도는 아래처럼 갱신한다. 이번 문서화와 설계 시험은 실제 데이터 변환이나 UI 구현의 완료를 뜻하지 않는다.

이 모델은 사람이 편집하는 수련 원본의 기준이다. 배포용 읽기 형식, 배포 전 검사·변환과 브라우저의 책임은 [배포 경로 설계](data-pipeline.md)에서 별도로 정한다.

### 필요한 정보

가능한 범위에서 다음 정보를 서로 구분해 기록한다.

- 상태에는 왼발·오른발 위치 `(x, y)`, 발별 허실·지지 역할, 발끝 방향과 현재 접촉을 담는다.
- 상태에는 중심 `C`의 점 또는 질적 영역, 몸 방향 `B`, 필요한 팔의 상대적 배치를 담는다.
- 동작에는 뒤꿈치 들기·내리기, 중심 이동, 회전 `ΔB` 같은 변화와 확인된 선후 관계를 담는다.
- 안내에는 동작 설명과 자기 점검 항목을 담는다.
- 해석에는 원리·음양·전투 가정과 적용 범위를 담는다.
- 각 정보에는 근거 유형, 위치, 근사 여부와 미확인을 구분해 기록한다.

### 개념 구조

```text
Dataset
  sections -> posture occurrences -> numbered motions
  coordinateFrames
  states
  interpretations
  sources

PostureOccurrence
  id, name
  start: initial / previousEnd
  motionIds[]
  introduction

Motion
  id, number
  endStateId
  checkpointStateIds[]
  events[], eventRelations[]
  instruction, checks[]

State
  id, coordinateFrameId
  feet.left / feet.right
  center
  body
  arms

Interpretation
  category: principle / yinYang / application
  scope: motion / group / posture
  availability
  content / reason

Claim
  value + basis + precision + evidence
  또는 unknown / not_applicable
```

### 체크

- [x] 현재 `lx / lt / rt / cx / shi` 구조의 한계를 정리한다.
- [x] 새 프레임 스키마를 문서로 정의한다. — `data-model.md` 0.1에 상태·동작·해석을 분리했다.
- [x] 모르는 값과 미등록 참조를 구분하는 규칙을 정한다. — 물리 필드는 Claim의 미확인으로, 미등록 결과 참조는 `null`로 표시한다.
- [x] 관찰값과 추론값을 구분하는 필드를 정한다. — 필드별 근거와 근사 여부를 기록한다.
- [x] 시작 상태·중간 상태·부분 미등록과 UI의 연결 규칙을 정의한다.
- [x] 가상 데이터로 핵심 설계 계약을 시험한다. — 로컬 Node.js에서 20개 시험을 통과했다. 전체 스키마·실제 권가·브라우저 검증은 아니다.
- [x] 좌표계 문서의 고정 축, 발 기준점, 상태와 사건의 구분을 새 설계와 맞춘다. — `coordinate-system.md` 1.3에 반영하고 `data-model.md`의 이행 상태를 맞췄다. 코드·실제 데이터 이행은 별도다.
- [ ] 수련 원본과 배포 결과 각각의 실행 스키마 및 배포 전 검증기를 작성한다. — 예비식 시범 범위는 구현했다. 전체 모델·운영 빌드 경로는 아래 2.1절을 따른다.
- [x] 기존 預備式 4동작을 새 구조로 변환한다. — 원문 재대조 전의 이전 후보이며 사부님 확인 완료본은 아니다.
- [ ] 기존 화면이 새 구조에서도 정보를 보존하는지 확인한다. — 달라지는 표현은 이유를 기록한다.

### 2.1 수련 원본·배포 데이터·디렉터리 분리

결정 문서는 [수련 원본 → 검사·변환 → 배포 결과](data-pipeline.md)다. 기존 동작 의미 모델은 유지하되 복잡한 참조 검사와 화면용 자료 연결은 배포 전에 끝낸다.

#### 설계와 준비

- [x] 원본 `training/`, 화면 `web/`, 자동 생성물 `dist/`의 역할과 편집권을 나눈다.
- [x] 수련 원본만 내용의 기준으로 삼고 배포본을 역으로 편집하지 않도록 정한다.
- [x] 변환 시 보존할 미확인·근사·추론·근거·해석 범위를 명시한다.
- [x] 원본 검사 → 참조 검사 → 변환 → 결과 검사 → 사이트 조립 → 배포의 경로를 정의한다.
- [x] GitHub Pages에는 성공한 dist만 전달하는 목표와 기존 방식의 전환 조건을 정한다.
- [x] `training/README.md`, `web/README.md`와 생성물 Git 제외 규칙을 추가한다. — 예비식 이전 후보는 이후 training에 등록했다. 화면 파일은 아직 옮기지 않았다.

#### 구현과 검증

- [x] 두 실행 스키마와 서로 대응하는 최소 입출력 예시를 만든다. — 사용자 선택에 따라 기존 예비식 4동작을 사용했다. `preparation-pilot` 범위이며 전체 모델 지원은 아니다. 가상 다음 자세는 시험 코드 안에서만 사용한다.
- [ ] 배포 전 검사·변환 명령을 구현하고 원본을 변경하지 않는지 확인한다. — 시범 검사·변환과 원본 불변성은 확인했다. 전체 사이트를 조립하는 `build.mjs`는 아직 없다.
- [ ] 성공·구조 오류·정상 미등록·불확실성 보존·배포 제외·재생성·실패 시 이전 결과 오용을 시험한다. — 예비식 쌍의 35개 시험을 통과했다. dist 승격·실패 시 배포 차단·브라우저는 별도다.
- [ ] 예비식 실제 원본을 training에 등록하고 기존 값의 의미와 근거를 대조한다. — 등록·기존 코드와의 대조는 완료했다. 외부 권보 원문과 사부님 확인은 남아 있다.
- [ ] 작업 브랜치에서 화면을 web으로 옮기고 배포 데이터만 읽도록 연결한다. — 루트의 목차와 설명을 별도 원본으로 계속 유지하지 않는다.
- [ ] file://, HTTP, /eztaiji/ 경로와 모바일·데스크톱에서 기본 화면 및 오류 안내를 확인한다.
- [ ] 실제 Pages 게시 소스와 Actions 작업 의존성을 확인하고 성공한 dist만 배포하도록 전환한다.
- [ ] 새 경로를 검증한 뒤 이전 루트 파일과 중복 데이터 제거를 마무리한다.

이 구현 체크는 2단계의 데이터 변환, 6단계의 UI, 7단계의 예비식 이행과 연결된다. 폴더 안내 작성만으로 해당 구현 단계들을 완료 처리하지 않는다.

### 2.2 예비식 원본·배포 쌍의 시범 구현

상세 기록은 [예비식으로 만든 데이터 쌍](preparation-pair.md)이다. 완료 범위는 자료 구조와 변환이며, 실제 기술 내용이나 운영 UI가 검증되었다는 뜻은 아니다.

- [x] 시작 상태 1개와 예비식 4동작을 원본에 기록한다. — 초기 신체 값은 미확인이다.
- [x] 기존 제목·설명·원리·자기 점검 문구를 보존한다.
- [x] C를 질적 영역으로, 발 좌표·방향을 설명용 근사로 옮긴다.
- [x] 접촉 상태와 들기·내리기 사건을 분리하고 원문 재확인 전 기호를 억제한다.
- [x] 음양 4개를 프로젝트 해석으로 표시하고 전투 가정은 미작성으로 남긴다.
- [x] 원본과 배포 형식의 스키마를 검사하고 필드별 의미 보존을 대조한다.
- [x] Node 21개·스키마 관련 14개, 총 35개 시험을 실행한다.
- [x] 원본 불변성·재생성 일치·작성용 정보 제외를 확인한다.
- [ ] 외부 권보와 실제 지도에 맞춰 미확인 항목을 재검토한다.
- [ ] 배포 결과를 실제 연습 화면에 연결한다.

### 2.3 공통 수련 원리 레이어

특정 자세 하나에 묶이지 않고 여러 동작에서 반복되는 사부님 설명은 `principles`에 한 번 정의하고 관련 동작이 ID로 참조한다. 소개 페이지에서는 전체 설명을 제공하고, 연습 화면에서는 실제로 연결한 동작에서만 짧게 다시 보여준다.

- [x] 전역 `principles` 컬렉션과 `Motion.principleIds[]` 참조를 정의한다.
- [x] 원본·배포 스키마와 변환기에 공통 원리 참조를 추가한다.
- [x] 첫 원리로 **경직하지 않고 변화 가능성을 유지하기**를 현장 수련 메모에 근거해 등록한다.
- [x] 預備式 1동작과 太極起勢 1–6동작에 공통 원리를 연결한다.
- [ ] 攬雀尾에서 같은 원리가 실제로 어떻게 드러나는지 현장 지도와 대조한다.

---

## 3. 음양 해석 원칙 만들기

목표: 음양을 단순한 좌우 대응이 아니라, 동작 안에서 실제로 변하는 관계로 설명한다.

### 원칙

- `왼쪽 = 陰 / 오른쪽 = 陽`처럼 고정하지 않는다.
- 해당 동작에서 실제로 드러나는 관계만 적는다.
- 모든 프레임에 억지로 음양 항목을 넣지 않는다.
- 하나의 프레임에 여러 음양 관계가 동시에 존재할 수 있다.

### 예시 관계

- `虛 ↔ 實`
- `開 ↔ 合`
- `進 ↔ 退`
- `收 ↔ 放`
- `상대의 進 ↔ 나의 化`
- `한쪽 공간을 비움 ↔ 다른 쪽 공간을 차지함`

위 예시에는 전통적 대립쌍과 프로젝트의 설명용 대비가 함께 있다. 작성 원칙에서 둘을 구분하며, 모든 대비를 표준 음양 분류로 단정하지 않는다.

### 체크

- [ ] 음양 해석 원칙을 별도 문서에 정의한다.
- [ ] 한 프레임에 여러 관계를 저장하는 형식을 정한다.
- [ ] 음양이 불분명한 경우 비워 두는 원칙을 명시한다.
- [ ] 預備式에서 실제로 유효한 음양 관계만 시험 입력한다. — 구조 시험용 프로젝트 해석은 입력했다. 실제 지도와의 유효성 대조는 별도다.

---

## 4. 전투 가정 해석 원칙 만들기

목표: 동작을 고정된 공격·방어 기술로 단정하지 않고, 상대와의 힘 관계 속에서 이해한다.

### 기본 구조

```text
상황
상대가 어떤 힘을 주는가?

대응
그 힘에 대해 내 몸 전체가 어떻게 반응하는가?

결과
힘·중심·공간의 관계가 어떻게 바뀔 수 있는가?

한계
어떤 전제에서 가능한 설명이며 무엇이 아직 확인되지 않았는가?
```

### 원칙

- 손 모양만 보고 공격·방어를 단정하지 않는다.
- 독립된 전투 용법이 아니라 다음 동작을 위한 구조 형성일 수 있다.
- 한 프레임이 아니라 여러 동작 묶음 전체가 하나의 용법일 수 있다.
- 사부님 설명, 문헌, 외부 영상, 프로젝트 해석을 구분한다.

### 범위

- `motion`: 한 동작 자체에서 의미가 충분히 드러남.
- `group`: 左掤 등 여러 동작 묶음에 걸쳐 의미가 완성됨. 일부에만 해당하면 대상 동작 ID를 명시한다.
- `posture`: 한 자세 전체를 봐야 의미를 설명할 수 있음.

### 체크

- [ ] 전투 가정 작성 규칙을 문서화한다.
- [ ] `scope: motion / group / posture` 규칙을 확정한다.
- [ ] 독립 용법이 없는 동작의 표기 방식을 정한다.
- [ ] 출처와 추론을 구분하는 규칙을 확정한다.
- [ ] 預備式에는 억지 용법을 붙이지 않고 준비 구조로 설명 가능한지 검토한다.

---

## 5. 해석 원칙 문서 추가

목표: 좌표계와 별도로, 음양·무술적 해석에 관한 공통 기준을 만든다.

예정 파일:

`docs/interpretation-principles.md`

포함 내용:

- 음양을 해석하는 방법
- 전투 가정을 어디까지 허용할지
- 관찰과 추론을 어떻게 구분할지
- 한 프레임과 여러 프레임의 용법을 어떻게 나눌지
- 사부님 설명과 외부 자료가 다를 때 무엇을 우선할지
- 불확실한 내용은 어떻게 표시할지

### 체크

- [ ] 초안 작성
- [ ] 預備式 사례를 넣어 검증
- [ ] 太極起勢 사례를 넣어 검증
- [ ] 실제 수련 중 사부님의 설명과 비교해 수정

---

## 6. 화면 구조 확장

목표: 현재의 간결한 모바일 연습 화면을 유지하면서, 원리·음양·전투 맥락을 함께 볼 수 있게 한다.

### 설명 영역 순서

1. **지금 어떻게 움직이나**
2. **왜 이렇게 움직일까?**
3. **여기서의 음양**
4. **전투 상황에서는**
5. **내 자세 확인**

### 표시 원칙

- 좌표판은 계속 고정한다.
- 긴 설명만 독립 스크롤한다.
- 해당 정보가 없으면 빈 카드나 억지 문장을 표시하지 않는다.
- 여러 동작에 걸친 설명은 범위를 명시한다.
- 출처가 필요한 해석은 근거를 접어 볼 수 있게 한다.

### 체크

- [ ] 시작 상태 UI를 추가한다.
- [ ] `여기서의 음양` 영역을 추가한다.
- [ ] `전투 상황에서는` 영역을 추가한다.
- [ ] `scope`가 group/posture일 때 범위를 표시한다.
- [ ] 작은 iPhone 세로 화면에서 스크롤과 버튼 동작을 확인한다.
- [ ] 태블릿·데스크톱 좌우 배치에서도 읽기 쉬운지 확인한다.

---

## 7. 預備式 마이그레이션

목표: 현재 4동작을 새 구조에 완전히 옮겨 첫 통합 검증 자료로 사용한다. 이전 후보의 데이터 작성과 변환은 2.2절에서 완료했으며, 아래는 내용 검토와 화면까지 포함한 통합 완료 기준이다.

### 체크

- [x] 預備式 시작 상태를 정의한다. — 兩腳跟併攏外八字, 右實左虛, 양팔 자연 하수 상태를 접근 가능한 기록에 근거해 입력했다.
- [ ] 4동작의 발 좌표를 새 구조로 옮긴다.
- [ ] `C`, `B`, 발끝 방향, 접촉 정보를 새 구조로 옮긴다.
- [ ] 기존 설명·원리·체크리스트를 옮긴다.
- [ ] 유효한 음양 관계를 추가한다.
- [ ] 전투 가정은 준비 구조 수준에서만 작성한다.
- [ ] 출처와 추론 구분을 확인한다.
- [ ] 기존 화면과 비교해 정보 손실이 없는지 확인한다.

---

## 8. 太極起勢 6동작 추가

목표: 새 구조가 실제로 발·중심·몸·팔의 연동을 다룰 수 있는지 시험한다.

### 확인할 것

- 시작 상태가 預備式 마지막 상태를 제대로 이어받는가?
- 발 좌표 변화가 거의 없는 동작도 잘 표현되는가?
- 중심의 상승·하강 또는 전후 이동을 어떻게 설명할 것인가?
- 팔의 움직임을 몸통과 분리하지 않고 설명할 수 있는가?
- 음양 관계가 단순 허실 이상으로 드러나는가?
- 전투 가정이 실제 자료를 넘어서 과장되지 않는가?

### 체크

- [x] 시작 상태 연결 — 預備式 4동작 결과 상태를 그대로 참조한다.
- [x] 1동작 — 두 손을 몸 앞 약 20도로 띄운다.
- [x] 2동작 — 두 팔을 어깨 높이까지 띄운다.
- [x] 3동작 — 손목 原空位를 유지하며 팔꿈치를 풀고 손바닥·손끝을 평평하게 세운다.
- [x] 4동작 — 계속 鬆肘하여 손등을 어깨 쪽으로 거두고 손목을 접는다.
- [x] 5동작 — 손끝 原空位를 유지하며 팔꿈치·손목을 침강시켜 立掌을 만든다.
- [x] 6동작 — 팔꿈치 原空位를 유지하며 손바닥을 앞아래로 천천히 침강시킨다.
- [x] 전체 흐름 검토 — 접근 가능한 易簡1–64式 기록의 6동작 순서와 직접 대조했다.
- [x] 데이터 구조 수정 필요 여부 판단 — 발 좌표 변화가 거의 없는 상태에서도 arms·instruction·interpretation으로 구분 가능해 현재 모델을 유지한다.

---

## 9. 攬雀尾 21동작 확장

목표: 좌표계와 해석 모델이 복잡한 실제 초식에도 작동하는지 검증한다.

### 묶음별 진행

- [x] 左掤 6동
- [x] 右掤 5동
- [x] 左履 2동
- [x] 右擠 3동
- [x] 雙按 5동

21동을 수련 원본과 배포 데이터에 연결했다. 현재 `preparation-pilot` 스키마는 구조적 `group` 범위 해석을 아직 지원하지 않으므로, 배포 화면에서는 `左掤 1/6` 같은 동작 제목으로 묶음을 표시한다. 자세 전체의 원리·음양·전투 가정은 `scope: posture`로 연결했다. 좌표판은 C·발·팔의 최종 자세에 집중한다. 팔은 절대 방향을 저장하지 않고 `body.heading`에 대한 `relativeDirection`을 저장하며, 빨간 화살표는 둘을 합성해 계산한다. 따라서 몸이 팔을 데리고 회전하는 경우와 팔 자체가 몸에 대해 변하는 경우를 구분할 수 있다. B와 고관절·허리·어깨·팔꿈치의 연결 과정은 상세 데이터로 남긴다.

각 묶음마다 아래를 확인한다.

- 발 위치와 허실
- 중심 이동
- 몸 방향
- 팔과 몸의 연결
- 음양 관계
- 전투 가정
- `motion / group / posture` 범위
- 출처와 추론 구분

---

## 10. 이후 확장

1~3자세에서 구조가 안정되면 나머지 64자세로 확장한다.

- [ ] 반복 자세의 데이터 재사용 방식을 정한다.
- [ ] 같은 이름의 자세라도 맥락에 따라 다른 시작 상태를 처리한다.
- [ ] 방향 전환이 큰 자세를 위한 좌표판 범위를 검토한다.
- [ ] 필요하면 손·팔의 상대적 위치 표현을 추가한다.
- [ ] 사부님 설명을 현장에서 빠르게 기록할 수 있는 입력 형식을 검토한다.

---

## 현재 다음 작업

**攬雀尾 21동작 실사용 확인 → 묶음 범위 지원 검토**

預備式 4동작, 太極起勢 6동작, 攬雀尾 21동작을 시작 상태부터 화면에 연결했다. 攬雀尾 좌표판은 C·발·팔을 우선 표시하고, 몸 방향 B와 관절 연결 과정은 상세 데이터로 둔다. 팔은 몸 기준 상대 방향으로 저장해 B와 합성한다. 실제 수련에서 사부님의 지도와 다른 부분이 발견되면 `training/datasets/yijian.json`을 먼저 고친다. 다음 구조 작업은 `scope: group` 지원과 다른 자세의 상대 팔 방향 이행을 검토하는 일이다.
